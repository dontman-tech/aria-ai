# ARIA - Core dependencies (always required)
pyyaml>=6.0

# Voice - speech recognition & synthesis
# Note: pyaudio may require system packages:
#   Debian/Ubuntu: sudo apt-get install portaudio19-dev python3-pyaudio
#   macOS: brew install portaudio
SpeechRecognition>=3.10.0
pyaudio>=0.2.13; sys_platform != "win32"
pyttsx3>=2.90

# Brain - LLM providers
# DeepSeek is the default provider (OpenAI-compatible API)
openai>=1.0.0
# anthropic>=0.40.0  # uncomment to use Claude

# Optional: higher-quality offline STT
# openai-whisper>=20231117

# Optional: Google TTS (higher quality, requires network)
# gTTS>=2.5.0

# System monitoring (for system_control skill)
psutil>=5.9.0

# Web UI (installable PWA dashboard)
flask>=3.0.0

# PWA icon generation (only needed if regenerating icons)
pillow>=10.0.0

